# Neo4j Connection Configuration
neo4j:
  uri: "bolt://localhost:7687"
  user: "neo4j"
  password: "omnigraph_password"
  database: "neo4j"

# Ingestion tuning
ingestion:
  batch_size: 10000
  max_retries: 3
  retry_delay_seconds: 2
  # Maximum concurrent Neo4j sessions
  max_sessions: 4
